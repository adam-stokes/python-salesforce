#!/usr/bin/env python
# vim:syntax=python
# sfc - cli to salesforce api
from sforce.client import SF
from sforce.models import Schema
from sforce.models.account import Account
from pprint import pprint

if __name__=="__main__":
    sfc = SF(sandbox=True)
    # Start a session
    sfc.session
    schema = Schema(sfc.commons)
    acct = Account
    pprint(acct().describe())
    pprint(acct().get('001M000000P0UzbIAF'))
